<div class="container mb-5 mt-5">
  <center>
  <h1>Menu Settings - <%= @menu.name %></h1>
    <small class="text-muted">View Stats, Edit and Delete Menu Actions</small>
  </center>
  <ul class="nav nav-tabs mt-3">
    <li class="nav-item">
      <a class="nav-link" href="/menus/<%= @menu.id %>/edit">Information</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="/menus/<%= @menu.id %>/logs">Logs</a>
    </li>
    <li class="nav-item">
      <a class="nav-link active" href="/menus/<%= @menu.id %>/settings">Settings</a>
    </li>
  </ul>
  <div class="container mt-5 center">
    <p class="mt-2">You can delete the entire menu below but note: it will delete all associated headers and line items bond to it!</p>
    <div class="row">
      <div class="col-4">
      </div>
      <div class="col-8">
        <ul class="list-group">
        <li class="list-group-item d-flex justify-content-between align-items-center">
          Users contributed
          <span class="badge badge-info badge-pill">1</span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          Total Errors
          <span class="badge badge-info badge-pill"><%=@errors.count %></span>
        </li>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          Page Views
          <span class="badge badge-primary badge-pill"><%= @menu.page_views %></span>
        </li>
      </ul>
    </div>
    </div>
</div>

<%= button_to "Delete This Menu", {:controller => :menus,
  :action => 'destroy', :id => @menu.id }, data: { confirm: "Are you sure you want to delete?" }, :method => :delete, class: "btn btn-danger btn-lg btn-block mt-5" %>
